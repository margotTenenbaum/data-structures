package sort;

public class quickSort {

    public static int[] quicksort(int[] input) {
        if (input.length == 0 || input.length == 1) {
            return input;
        }
        return input;
    }
}